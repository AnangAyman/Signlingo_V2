# Keep only the top-level packages we intentionally depend on here.
# Their transitive dependencies are installed automatically by pip.

# Django app runtime
Django==5.2.4
dj-database-url==3.0.1
email-validator==2.2.0
gunicorn==22.0.0
Jinja2==3.1.6
psycopg[binary]==3.2.9
whitenoise==6.9.0

# ML/runtime packages still required by the Django prediction flow
h5py==3.13.0
mediapipe==0.10.18
numpy==1.26.4
opencv-python==4.11.0.86
pillow==11.2.1
pytz==2025.2
tensorflow==2.19.0
